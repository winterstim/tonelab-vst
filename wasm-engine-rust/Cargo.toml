[package]
name = "tonelab_wasm_engine"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
atomic_float = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
